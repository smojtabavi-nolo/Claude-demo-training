{
  "name": "demo-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Auth/session demo app for the Claude Code one-day intensive. The running example across the course is a refresh-token rotation bug.",
  "scripts": {
    "test": "vitest run",
    "demo": "tsx src/demo.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
